const initialState = {
    items: {},
    errors: [],
  };
  
  export const commonActions = {
    INITIALIZE_DATA: (state) => {
      return initialState;
    },
    UNMOUNT: (state) => {
      return initialState;
    },
  };
  